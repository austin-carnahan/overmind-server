# Copy to .env (ignored by git).
#
# TEMPORARY until the SSD is attached: this lands on the microSD (~108 GB
# free, limited write endurance). Do not run sustained/large downloads against
# it. Move to an SSD-backed path once available and update this value.
TRANSMISSION_DOWNLOAD_ROOT=/home/austin/tmp-downloads
