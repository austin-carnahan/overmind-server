# Copy to .env (ignored by git).
#
# SSD-backed. No fixed logical path name for this one (unlike /mnt/substrate
# etc.), so it points directly at a directory on the SSD mount rather than
# going through a bind mount.
TRANSMISSION_DOWNLOAD_ROOT=/mnt/ssd/downloads
