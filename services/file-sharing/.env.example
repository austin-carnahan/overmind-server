# Copy to .env (ignored by git).

# A dedicated Samba account scoped to this one share — not the austin Linux
# login, and not the account's master key/password for anything else.
SAMBA_PASSWORD=CHANGE_ME
