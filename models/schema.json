{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cerebrate Pixel 6 model catalog",
  "description": "Stage 1 of the Operational Model Catalog (design-notes/cerebrate_pixel6_operational_model_catalog_v4.md, Phase A). Deliberately small: validates the concept against the two models already in production before extending to anything new.",
  "type": "object",
  "required": ["models"],
  "additionalProperties": false,
  "properties": {
    "models": {
      "type": "object",
      "additionalProperties": { "$ref": "#/definitions/model" }
    }
  },
  "definitions": {
    "model": {
      "type": "object",
      "required": ["capabilities", "artifacts", "worker", "residency"],
      "additionalProperties": false,
      "properties": {
        "capabilities": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" },
          "description": "Public capability names, per catalog v4 Section 10 (e.g. classify-image, chat)."
        },
        "description": { "type": "string" },
        "artifacts": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/definitions/artifact" },
          "description": "Keyed by deployment artifact type, e.g. tflite, litertlm, onnx."
        },
        "worker": { "$ref": "#/definitions/worker" },
        "execution_preferences": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": { "type": "string" }
          },
          "description": "Keyed by device (e.g. pixel6), ordered most- to least-preferred backend identifiers."
        },
        "residency": {
          "type": "string",
          "enum": ["pinned", "on_demand", "manual"],
          "description": "Per catalog v4 Section 9. Applied at the worker-process level, not in-process."
        },
        "requirements": { "$ref": "#/definitions/requirements" }
      }
    },
    "artifact": {
      "type": "object",
      "required": ["source"],
      "additionalProperties": false,
      "properties": {
        "source": { "$ref": "#/definitions/source" },
        "validated": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "enum": ["untested", "pending", "passed", "failed", "experimental"]
          },
          "description": "Keyed by device_backend, e.g. pixel6_nnapi. Measured status only -- never assumed."
        },
        "api_requirements": {
          "type": "object",
          "additionalProperties": false,
          "required": ["modality", "litertlm_surface"],
          "description": "Only meaningful for litertlm artifacts. Found necessary after granite-docling-258M (litert-community): its multimodal .litertlm cannot be driven through the plain C API's SessionAdvanced path at all (verified against the real LiteRT-LM source, not assumed) -- only through the full C++ SDK's InputImage(TensorBuffer) construction, or the Kotlin/Swift app layers. Recording this per-artifact so that gap isn't rediscovered per model.",
          "properties": {
            "modality": {
              "type": "string",
              "enum": ["text", "vision", "audio", "multimodal"]
            },
            "litertlm_surface": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "enum": ["c_session", "cpp_session_advanced", "kotlin_conversation", "swift_conversation"]
              },
              "description": "Which integration surface(s) this artifact's modality actually requires. c_session (engine.h's plain C API) is confirmed multimodal-incapable -- it can only construct InputImage from raw encoded bytes, which SessionAdvanced's PreprocessContents rejects for anything but text. cpp_session_advanced is the verified-real, heavier full-C++-SDK path (InputImage(TensorBuffer), CMake+Abseil-cpp build, same shape as the LiteRT CompiledModel spike's C++ SDK path) that Cerebrate's existing native-worker architecture could still reach without an Android app. kotlin_conversation/swift_conversation are the model card's only currently-documented working paths, both requiring an actual app, not a native binary."
            }
          }
        },
        "cerebrate_support": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "native_c_session": {
              "type": "boolean",
              "description": "Whether this artifact's required litertlm_surface is reachable from the existing plain-C-API native worker pattern (cerebrate-generate) without further build/architecture changes."
            },
            "validated": {
              "type": "boolean",
              "description": "Whether native_c_session was actually exercised end-to-end on-device, not assumed from reading the API surface alone."
            }
          }
        }
      }
    },
    "source": {
      "type": "object",
      "description": "Exactly one provenance shape: a pinned repo revision (e.g. Hugging Face), or a pinned direct-download URL with a checksum.",
      "oneOf": [
        {
          "type": "object",
          "required": ["repo", "revision"],
          "additionalProperties": false,
          "properties": {
            "repo": { "type": "string" },
            "revision": { "type": "string" }
          }
        },
        {
          "type": "object",
          "required": ["url", "sha256"],
          "additionalProperties": false,
          "properties": {
            "url": { "type": "string", "format": "uri" },
            "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
          }
        }
      ]
    },
    "worker": {
      "type": "object",
      "required": ["binary", "port"],
      "additionalProperties": false,
      "properties": {
        "binary": {
          "type": "string",
          "description": "Which Android worker process type serves this model, e.g. cerebrate-infer, cerebrate-generate."
        },
        "port": { "type": "integer", "minimum": 1, "maximum": 65535 }
      }
    },
    "requirements": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "disk_mb": { "type": "number" },
        "ram_mb": {
          "type": ["number", "null"],
          "description": "null where genuinely not yet measured -- do not fill in a guessed value."
        }
      }
    }
  }
}
